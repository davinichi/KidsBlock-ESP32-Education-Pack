# ライブラリガイド

## KidsBlock ESP32 Education Pack Version 0.5 Preview

## Communication

### WiFi Simple

ESP32をWi-Fiへ接続するための拡張です。KidsBlock標準Wi-Fiブロックのように不要なTCPサーバーを起動せず、Wi-Fi接続の役割に限定しています。

### HTTP Server

ESP32のセンサーデータをスマートフォンやPCのブラウザへ表示し、ブラウザから操作できます。

主な機能：

- 最大8件の表示データ
- 最大8系統のON・OFFボタン
- 最大8項目のラジオボタン
- 選択番号・状態の取得
- 未登録セクションの自動非表示
- 通信状態の常時表示

### ThingSpeak

ThingSpeakへデータを送信し、最新のフィールド値を取得します。クラウド上での記録やグラフ表示に利用できます。

### BLE UART 3

BLEによる文字列通信を行います。PC、Chromebook、対応スマートフォンなどとの通信教材に利用できます。

### ESP-NOW ESP32

Wi-Fiルーターを使わずにESP32同士で文字列を送受信します。

- 送信先MACアドレスが空欄：ブロードキャスト
- MACアドレスを入力：個別送信
- 受信文字列と送信元MACアドレスを取得
- 新規受信と送信結果を条件判定

初版はESP-NOW単独利用を基本としています。

## Other

### Environment

温度・湿度から湿球温度、簡易WBGT、警戒レベルなどを計算します。

### NTP Clock

NTPサーバーから日時を取得します。時計表示、ログの時刻、クラウド送信時刻などに利用できます。

### KBSD ESP32

microSDカードをSPI接続で利用します。

- 初期化
- カード検査
- ファイル書込み・追記・読込み
- ファイル削除・存在確認
- 1行ずつ読込み
- 次の行の有無の判定

### Data Processing

CSV分解と文字列処理を行います。

- 見出し行数の指定（初期値1）
- 最大8項目のCSV分解
- 左右からの文字抽出
- 指定位置から末尾までの抽出
- 指定位置から指定文字数の抽出
- 文字列長
- 任意の区切り文字による文字列結合

### ESP32 System Information

ESP32のシステム情報を値ブロックで取得します。

- チップモデル、リビジョン、コア数、CPU周波数
- Arduino ESP32 Core、ESP-IDF
- Wi-Fi MACアドレス
- フラッシュ容量・速度
- スケッチ使用量・空き領域
- ヒープ総量・空き・最小空き・最大連続領域
- PSRAM容量・空き容量
- 稼働時間

値ブロックは標準のシリアル表示、LCD、HTTP Server、SD保存などへ渡せます。
