# インストールガイド

## KidsBlock ESP32 Education Pack Version 0.5 Preview

## 1. インストール前の重要事項

本パッケージはVersion 1.0公開前のPreview版です。すべての環境での正常動作を保証するものではありません。

インストール前に、次の内容を確認してください。

- KidsBlockを終了する
- 重要なプログラムやデータをバックアップする
- `LICENSE_JA.md`の利用条件と免責事項を読む
- Preview版であることを理解する

`install.bat`で`Y`を入力して続行した場合、`LICENSE_JA.md`の内容に同意したものとみなされます。同意できない場合は`N`を入力して中止してください。

## 2. インストール手順

1. 配布ZIPを展開します。
2. 展開したフォルダを開きます。
3. `install.bat`を実行します。
4. ライセンスと免責事項を確認します。
5. 同意する場合は半角の`Y`、同意しない場合は`N`を入力します。
6. パッケージ検証とコピーが完了するまで待ちます。

正常終了例：

```text
Installation completed successfully.
Installed 10 extensions.
Version: 0.5-Preview
```

## 3. 同意しない場合

`Y`以外を入力した場合、PowerShellインストーラーは起動せず、拡張ライブラリはコピーされません。

## 4. 重要な運用上の注意

通常のインストールでは、必ず`install.bat`を使用してください。

`tools\install.ps1`を直接実行すると、`install.bat`のライセンス確認画面を通りません。一般利用者向けの手順として、PowerShellスクリプトの直接実行はサポートしません。

## 5. インストール結果

インストール処理の結果は`install_result.txt`へ記録されます。

## 6. トラブルシューティング

### ライセンス確認後にインストールされない

- 半角の`Y`を入力してください。
- `LICENSE_JA.md`が配布フォルダの直下にあることを確認してください。
- ZIP内から直接実行せず、展開後のフォルダから実行してください。

### PowerShellで構文エラーが出る

今回の修正版では、ライセンス確認処理を`install.ps1`へ追加していません。正常動作していた`tools\install.ps1`が配置されていることを確認し、この更新一式の両ファイルをセットで上書きしてください。
